config.username=foobar
config.password=1234
config.host=localhost
config.port=32768
config.database=mongo-connector-test
config.name=
#config.connectTimeout= (optional)
#config.socketTimeout= (optional)
#config.connectionsPerHost= (optional)
#config.threadsAllowedToBlockForConnectionMultiplier= (optional)
#config.maxWaitTime= (optional)
#config.autoConnectRetry= (optional)

#Different database used for calls related to drop and restore
config.host.drop.restore=localhost
config.port.drop.restore=32768
config.database.drop.restore=
