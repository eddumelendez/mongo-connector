#Database used for all calls except those related to drop and restore
mongo.host=localhost
mongo.port=27017
mongo.database=test

#Different database used for calls related to drop and restore
mongo.host.drop.restore=localhost
mongo.port.drop.restore=27017
mongo.database.drop.restore=test

#Credentials are the same for both databases
mongo.username=admin
mongo.password=